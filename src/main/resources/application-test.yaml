spring:

  flyway:
    enabled: true

    locations:
      - classpath:db/migration
      - classpath:db/testdata

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: validate