apiVersion: v1
kind: Service

metadata:
  name: oficina360-api
  namespace: oficina360

spec:

  selector:
    app: oficina360-api

  ports:

    - port: 8080
      targetPort: 8080
      nodePort: 30080

  type: NodePort