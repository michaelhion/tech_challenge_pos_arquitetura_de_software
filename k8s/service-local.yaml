apiVersion: v1
kind: Service
metadata:
  name: oficina360-api
  namespace: oficina360

spec:
  type: NodePort

  selector:
    app: oficina360-api

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080