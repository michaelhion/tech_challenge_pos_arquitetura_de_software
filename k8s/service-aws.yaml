apiVersion: v1
kind: Service
metadata:
  name: oficina360-api
  namespace: oficina360

spec:
  type: LoadBalancer

  selector:
    app: oficina360-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080